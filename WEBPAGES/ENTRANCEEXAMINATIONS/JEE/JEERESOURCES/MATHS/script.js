function showSubject(subject) {
    // You can implement logic to filter reviews based on the selected subject
    // For simplicity, this example does not include dynamic content filtering
    alert(`Showing reviews for ${subject}`);
}
